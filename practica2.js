class Ordenador {
  constructor(marca, modelo, ram, capacidad, pulgadas) {
    this.marca = marca; //Texto
    this.modelo = modelo; //Texto
    this.ram = ram; //Numero
    this.capacidad = capacidad; //Numero
    this.pulgadas = pulgadas; //Número
  }
}
